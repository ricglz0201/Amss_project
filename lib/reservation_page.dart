import 'package:flutter/material.dart';

class ReservationPage extends StatelessWidget {
  ReservationPage();

  @override
  Widget build(BuildContext context) {
    return Text('Aqui se pueden reservar los asientos');
  }
}